import { PeopleSortPipe } from './people-sort-pipe';

describe('PeopleSortPipe', () => {
  it('create an instance', () => {
    const pipe = new PeopleSortPipe();
    expect(pipe).toBeTruthy();
  });
});
